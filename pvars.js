var vars = {
    babyFat: 9,
    feedSize: 2,
    wasteSize: 1,
    biteSize: 3, 
    decaySize: 1,
    minColor: 16,
    prefTypes: 'rgb',
    // r, g, b, R, G, B (low: r, g, b; high: R, G, B)
    // 1, 2, 3, 4 (up, right, down, left)
    bases: 'rgbRGB1234'
};
